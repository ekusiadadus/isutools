# HTTP defaults/backend fragment. HAProxy timing fields are milliseconds.
# Query strings are removed with %[path]; +Q JSON-quotes string samples.
log-format '{"schema":"isutools.http-access.v1","method":%{+Q}HM,"uri":%{+Q}HP,"protocol":"HTTP/1.1","status":%ST,"duration_ms":%Ta,"bytes":%B}'
