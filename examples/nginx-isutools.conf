# Include inside nginx's http {} block, then add
# `access_log /var/log/nginx/isutools.log isutools;` to the measured server.
# X-Isutools-Session must be a short pseudonymous ID produced by the app/test
# client. Never map a raw Cookie or authorization token into sess.
# X-Isutools-Scenario is a non-secret label such as login_and_browse.
# These request headers are spoofable measurement labels, never authorization.
# For real users, strip public values and overwrite them in a trusted app/proxy.
log_format isutools escape=json
  'method:$request_method'
  '\turi:$uri'
  '\tstatus:$status'
  '\treqtime:$request_time'
  '\tupstime:$upstream_response_time'
  '\tbytes:$body_bytes_sent'
  '\tcache:$upstream_cache_status'
  '\tctype:$sent_http_content_type'
  '\tproto:$server_protocol'
  '\tsess:$http_x_isutools_session'
  '\tscenario:$http_x_isutools_scenario';
